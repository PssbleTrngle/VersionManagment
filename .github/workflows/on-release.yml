name: On Release

on:
  release:
    types: 
      - created
      - updated

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      
      - run: echo '${{ github.ref }}'
